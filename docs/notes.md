# Research Notes

The project is a survey, not an original model-training study. Reproducible artifacts therefore focus on:

- verified bibliography
- taxonomy tables
- benchmark categorization
- transparent limitations
- scripts that regenerate local tables

If original experiments are added later, the repository must also include preprocessing, training, evaluation, inference, configs, random seeds, hardware/software descriptions, and raw result logs.

